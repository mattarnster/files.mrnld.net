<h2>Delete file?</h2>
{{#if isImage }}
<img src="/preview/{{upload.uploadId}}" alt="{{upload.fileName}}" />
{{else}}
<p>No preview available</p>
{{/if}}
<form action="/delete/{{upload.uploadId}}" method="POST">
    <input type="submit" value="Delete" />
    <input type="hidden" name="_csrf" value="{{csrfToken}}"/>
</form>